// Define a condition
let condition = true;

// Start of the flow
document.write("Process started");

// Check the condition
if (condition) {
    // If condition is true, execute this block
    document.write("Condition is true. Executing conditional code...");
    // Conditional code
    document.write("Conditional code executed");
} else {
    // If condition is false, skip conditional code
    document.write("Condition is false. Skipping conditional code...");
}

// End of the flow
document.write("Process ended");
